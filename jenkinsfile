pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                echo ' it is a check out section'
            }
        }

        stage('Build') {
            steps {
                // Replace this with your build commands
                echo 'echo "Building the project..."'
            }
        }

        stage('deploy'){
            steps{
                echo ' deploying stage is done'
            }
        }
    }
}
